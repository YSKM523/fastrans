# Changelog

版本号规则:`主版本.次版本.修订号`(语义化版本)。每个 Release 附 Windows x64 一键包(zip 内含 exe + 模型,解压即用)。

## v0.1.5 — 2026-08-25

- **简拼输入**(像微软拼音):打每个音节的首字母即可出词——`hl`→回来、`dw`→等我、`bj`→北京、`zmb`→怎么办,覆盖全部 6.5 万词库词条;纯简拼时直接驱动翻译预览,与全拼混排时缀于候选尾部;配合记忆功能,选过一次的简拼下次自动置顶

## v0.1.4 — 2026-08-25

- **北美白领口吻**:译文自动去教科书腔——缩写化(cannot→can't、we will→we'll、it is→it's 等 60+ 条词边界安全规则)、去公文短语(in order to→to、as soon as possible→ASAP),不改动语义;`style=0` 可关(直译党)
- **现代交互动效**:等待翻译时旋转圈 + 循环点点;回车而译文未就绪时"⟳ 正在上屏…";新译文淡入、重译中旧文变暗——全部动效只在有事发生时渲染,空闲零开销

## v0.1.3 — 2026-08-25

内置拼音输入升级:记忆、联想、候选翻页。

- **记忆**:选过的词/句按拼音记住(`%APPDATA%\fastrans\userdict.txt`,原子写入,自动剪枝),同样拼音下次排最前;整句转换也按使用频次加权(有上限,防止碎片化),完全没用过时零开销
- **联想**:选词后按历史搭配显示后续词(鼠标点击上屏,连续成句;绝不抢键盘按键)
- **候选翻页**:候选扩到 45 个,`-`/`=`(或 `,`/`.`、↑↓/PgUp/PgDn)翻页,页码指示,数字/空格选当前页,候选可点击
- 超长译文改为滚动区域,不再溢出窗口
- 修复:拦截仅在真正组拼音时生效(打 "GPT-4"、"Wi-Fi" 等英文不再被吞字符);选词后光标保证在末尾;数字 6-9 不再选中不可见候选;修饰键+方向键不再被误吞

## v0.1.2 — 2026-08-25

- **Windows 安装包**(Inno Setup):中文安装向导,装到用户目录(免管理员权限,与自动更新兼容),可选开机自启/桌面图标,系统"应用列表"可卸载;安装/卸载时自动结束运行中的实例
- Release 同时提供安装包与绿色版 zip

## v0.1.1 — 2026-08-25

- **自动更新**:启动时后台静默检查新版本,有则下载增量更新包(仅 exe,~25MB)原地替换,下次启动即新版。断网/超时完全无感;`%APPDATA%\fastrans\config.txt` 里 `autoupdate=0` 可关闭
- 输入框提示显示当前版本号
- Release 附带自更新资产 `fastrans.exe.gz` 与其 SHA256

## v0.1.0 — 2026-08-25

首个公开版本。

### 功能
- 全局热键呼出悬浮翻译条:打中文,实时显示英文,回车自动粘贴进原应用
- 完全离线:本地 opus-mt zh→en 模型(CTranslate2 int8 量化,oneDNN 后端),短句 ~30ms、长句 ~100ms
- 内置拼音输入(可 `Ctrl+P` 开关):无中文输入法的电脑直接打拼音,数字选词、空格选首选、回车整句转换(词频 Viterbi)
- 系统输入法完整支持:拼音内联组合、候选窗跟随光标
- 热键冲突自动降级:`Ctrl+Alt+Space` → `Ctrl+Shift+Space` → `Ctrl+Alt+E`,可用 `FASTRANS_HOTKEY` 指定
- 悬浮条可拖动(按住空白处),位置与设置持久化于 `%APPDATA%\fastrans\config.txt`
- `Ctrl+Q` 退出;剪贴板粘贴后自动还原

### 技术
- Rust 单二进制(egui 界面 + CTranslate2 引擎静态链接),无运行时依赖
- 引擎在工作线程加载并预热,启动约 1 秒内热键即可用
- 翻译竞速去重:过期结果不上屏,相同文本不重译
